# Method 1

Binding the tool as a `~/.local/bin` command.

Simply run this command lines

```sh
chmod +x install.sh
./install.sh
```

the command will only be installed as personal tool for *your* user.

### Uninstall

simply run 

```sh
./install.sh --uninstall
```